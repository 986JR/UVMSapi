spring.application.name=apiuvms 

# PostgreSQL DataSource
spring.datasource.url=jdbc:postgresql://localhost:5432/apiuvmsdb
spring.datasource.username=postgres
spring.datasource.password=Jay9Nine!
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update 

# have to be removed in production
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# JWT Configuration
jwt.secret=uvmsSecretKeyForJWTTokenGenerationThatShouldBeLongEnoughAndSecure2024
jwt.expiration=86400


logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG


spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
